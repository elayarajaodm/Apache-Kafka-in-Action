# Producing messages
. Create a topic
```bash
$ kafka-topics.sh \
    --create \
    --topic products.prices.changelog \
    --partitions 1 \
    --replication-factor 1 \
    --bootstrap-server localhost:9092
Created topic products.prices.changelog.
```
. Sends a messages to a topic
```bash
$ echo "coffee pads 10" | kafka-console-producer.sh \
    --topic products.prices.changelog \
    --bootstrap-server localhost:9092
```

# Consuming messages

```bash
$ kafka-console-consumer.sh \
    --topic products.prices.changelog \
    --bootstrap-server localhost:9092
# Press Ctrl-C to cancel
Processed a total of 0 messages
```
To display already written data we have to use the flag --from-beginning:
```bash
$ kafka-console-consumer.sh \
--topic products.prices.changelog \
--from-beginning \
--bootstrap-server localhost:9092
coffee pads 10
# Press Ctrl-C to cancel
Processed a total of 1 messages
```


# Consuming and producing messages in parallel

```bash
# Don't forget: use Ctrl-C to stop the consumer
$ kafka-console-consumer.sh \
    --topic products.prices.changelog \
    --bootstrap-server localhost:9092
```

To simulate the price changes, we now start kafka-console-producer.sh. The command doesn’t stop until we press Ctrl-D, sending the EOF (End of File) signal to the producer:
```bash
# Don't forget: use Ctrl-D to stop the producer
$ kafka-console-producer.sh \
--topic products.prices.changelog \
--bootstrap-server localhost:9092
The kafka-console-producer.sh command sends one message to Kafka per line we write. That means we can now type messages into the terminal with the producer:
```

### Producer window
> coffee pads 11 <br>
> coffee pads 12 <br>
> coffee pads 10 <br>
We should also see these promptly in the consumer window:


### Consumer window
coffee pads 11 <br>
coffee pads 12 <br>
coffee pads 10 <br>


### Consumer 2 window
```bash
$ kafka-console-consumer.sh \
--topic products.prices.changelog \
--from-beginning \
--bootstrap-server localhost:9092
coffee pads 10
coffee pads 11
coffee pads 12
coffee pads 10
```

### Producer 2 window
```bash
``$ kafka-console-producer.sh \
--topic products.prices.changelog \
--bootstrap-server localhost:9092
> pillow 30
> blanket 40
>
```
### Consumer 1 window
[...] <br>
pillow 30 <br>
blanket 40 <br>
### Consumer 2 window
[...] <br>
pillow 30 <br>
blanket 40 <br>
